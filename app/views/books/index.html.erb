<%= render "books/partials/header", header: "Books!" %>

<ul>
  <% @books.each do |book| %>
    <li>
      <%= link_to book.title, book %>
    </li>
  <% end %>
</ul>

<%= link_to "New Book", new_book_path %>