<%= render "books/partials/header", header: @book.title %>

<p> <%= @book.author %> </p>
<p> <%= @book.pages %> </p>
<p> <%= @book.minutes %> </p>
<p> <%= @book.status %> </p>

<%= link_to "Edit", edit_book_path(@book) %> | <%= link_to "Delete", book_path(@book), method: :delete, data: { confirm: "Are you sure?" } %> | <%= link_to "Back", books_path %>